services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    environment:
      PORT: 3000

  frontend:
    build: ./frontend
    ports:
      - "4200:4200"
    depends_on:
      - backend
